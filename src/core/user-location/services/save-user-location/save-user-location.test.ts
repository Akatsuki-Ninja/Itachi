import { describe, it } from 'node:test'

describe('Save User Location', () => {
  it("should create location when location don't exist", () => {
    /**
     * @todo
     * 1) create user
     * 2) call function
     * 3) check that only one location exits for this user
     */
  })
})
